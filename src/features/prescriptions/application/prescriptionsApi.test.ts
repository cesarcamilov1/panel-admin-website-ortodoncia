import { describe, expect, it, vi } from 'vitest'
import type { HttpTransport } from '../../../shared/api/http'
import { createPrescriptionsApi } from './prescriptionsApi'
const patientId = '11111111-1111-1111-1111-111111111111'; const prescriptionId = '22222222-2222-2222-2222-222222222222'
function transport() { return { get: vi.fn().mockResolvedValue({ items: [] }), post: vi.fn().mockResolvedValue({}), put: vi.fn(), patch: vi.fn().mockResolvedValue({}), del: vi.fn(), upload: vi.fn(), download: vi.fn() } as unknown as HttpTransport }
describe('PrescriptionsApi', () => { it('keeps every transition JSON-versioned and scoped to one patient', async () => { const http = transport(); const api = createPrescriptionsApi(http); await api.list(patientId); await api.update(prescriptionId, 2, 'Tomar con alimentos'); await api.addItem(prescriptionId, 3, { lineNumber: 1, medication: 'Ibuprofeno', presentation: '', dose: '400 mg', route: '', frequency: 'Cada 8 horas', duration: '3 días', instructions: '' }); await api.issue(prescriptionId, 4); await api.void(prescriptionId, 5); expect(http.get).toHaveBeenCalledWith(`/api/v1/prescriptions?patient_id=${patientId}`, undefined); expect(http.patch).toHaveBeenCalledWith(`/api/v1/prescriptions/${prescriptionId}`, { instructions: 'Tomar con alimentos', expected_version: 2 }, { ifMatch: 2 }); expect(http.post).toHaveBeenCalledWith(`/api/v1/prescriptions/${prescriptionId}/issue`, { expected_version: 4 }, { ifMatch: 4 }); expect(http.post).toHaveBeenCalledWith(`/api/v1/prescriptions/${prescriptionId}/void`, { expected_version: 5 }, { ifMatch: 5 }) }) })
