import { describe, expect, it, vi } from 'vitest'
import type { HttpTransport } from '../../../shared/api/http'
import { createOrthodonticsApi } from './orthodonticsApi'
const patientId = '11111111-1111-1111-1111-111111111111'; const caseId = '22222222-2222-2222-2222-222222222222'
function transport() { return { get: vi.fn().mockResolvedValue({ items: [] }), post: vi.fn().mockResolvedValue({}), put: vi.fn(), patch: vi.fn(), del: vi.fn(), upload: vi.fn(), download: vi.fn() } as unknown as HttpTransport }
describe('OrthodonticsApi', () => { it('uses patient cursor pagination and JSON bodies for versioned case and visit changes', async () => { const http = transport(); const api = createOrthodonticsApi(http); await api.listCases(patientId, caseId); await api.transitionCase(caseId, 2, 'ACTIVE'); await api.addVisit(caseId, 3, { appointmentId: '', encounterId: '', upperArchwire: '0.16', lowerArchwire: '', elastics: '', procedures: '', observations: '', nextVisitNotes: '' }); expect(http.get).toHaveBeenCalledWith(`/api/v1/orthodontic-cases?patient_id=${patientId}&after_id=${caseId}&limit=25`, undefined); expect(http.post).toHaveBeenCalledWith(`/api/v1/orthodontic-cases/${caseId}/status`, { status: 'ACTIVE', expected_version: 2 }, { ifMatch: 2 }); expect(http.post).toHaveBeenCalledWith(`/api/v1/orthodontic-cases/${caseId}/visits`, { upper_archwire: '0.16', lower_archwire: undefined, elastics: undefined, procedures: undefined, observations: undefined, next_visit_notes: undefined, expected_version: 3 }, { ifMatch: 3 }) }) })
